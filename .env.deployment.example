# Copy to .env.deployment.local (ignored by Git). Do not send credentials in chat.
STORAGE_PROVIDER=supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=daily-reading
# Optional: allows deployment through Render's official API when browser control is unavailable.
RENDER_API_KEY=
BACKEND_ACCESS_TOKEN=
GEMINI_API_KEY=
GEMINI_VOCAB_MODEL=
